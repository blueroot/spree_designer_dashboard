<div class="row">
  <div class="js-filter-search">
    <% if taxon_filters.present? %>
        <% params[:board_id] = board.id %>
        <% taxon_filters.each do |key, value| %>

            <div class="filter-option-box col-md-3">
              <%= select_tag key.to_sym, options_for_select(generate_filter_select(value, params, key)), class: 'form-control js-select-filter-value' %>
            </div>
        <% end %>
        <div class="js-filter-selected col-md-12">
        <% if params[:s].present? %>
            <% params[:s].each do |set| %>
            <%= link_to product_result_rooms_path(define_params_filter(params, set[0], set[1], :s, true)), class: 'js-remove-filter-rooms my-btn-primary' do %>
                      <%= set[1]%>
                      <i class="fa fa-close"></i>
                  <% end %>
            <% end %>
        <% end %>
        </div>
    <% end %>

  </div>
</div>