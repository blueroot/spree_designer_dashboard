
//$('#board_search_panel').hide()
//$('#board_search_panel').addClass('hidden')
//$('#board_search_panel').hide();
$('#select-products-box').html('<%= escape_javascript(raw render(:partial => "product_results", :locals => {:products => @products})) %>')
//$('.select-products-box').addClass('animated fadeInUp')
$('#select-products-pagination').html('<%= escape_javascript(raw render(:partial => "product_results_pagination", :locals => {:products => @all_products})) %>')
//$('#select-products-box').html('hh')


$('.board-lightbox-product').draggable({
	containment: '.main',
	cursor: 'move',
	helper: 'clone',
	appendTo: '#board-canvas',
	revert: 'invalid',
	start: function(event, ui) {
		isDraggingMedia = true;
	},
	stop: function(event, ui) {
		isDraggingMedia = false;
	}
});

$('.add-to-board-link').click(function() {
	//alert('product id:' + $(this).data('productId') + ' --- board_id: ' + $('#canvas').data('boardId'))
	getProductDetails($(this).data('productId'), $('#canvas').data('boardId'), null)
	//showProductDetails($(this))
	//handleCloneAndAddEvent($(this))
	
});

$('.bookmark-product').click(function() {
	addProductBookmark($(this).data('productId'))
});
$('.remove-bookmark-product').click(function() {
	removeProductBookmark($(this).data('productId'))
});


//setHeight();
//// Re-initialize the results pane on resize
//$(window).resize(function() {		
//	setHeight();
//	
//});